const ping = require('./ping')
const who = require('./who')
const whois = require('./whois')
const dice = require('./dice')
const joke = require('./300')


module.exports = {
    ping,
    who,
    whois,
    dice,
    joke
}
